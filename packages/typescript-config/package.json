{
	"name": "@anscer/typescript-config",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"typecheck": "tsc --noEmit --emitDeclarationOnly false",
		"format": "biome format --write .",
		"lint": "biome check .",
		"lint:fix": "biome check --fix ."
	},
	"devDependencies": {
		"typescript": "^5.8.3"
	}
}
